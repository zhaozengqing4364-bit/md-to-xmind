# md-to-xmind

A Skill Hub demo skill — turns Markdown documents into XMind-importable format,
or creates new XMind-importable mind maps from user requirements.

## Install
```bash
npx skillhub add zhaozengqing4364-bit/md-to-xmind --agent claude-code --global
```

See [SKILL.md](./SKILL.md) for the full skill definition.
